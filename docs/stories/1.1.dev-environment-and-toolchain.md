# Story 1.1 — Dev environment and toolchain

**Status:** Done
**Epic:** 001 — Foundation and measurement
**Stage:** infra
**Depends on:** none
**Branch:** `story/1.1-dev-environment-and-toolchain`

## Context
The repo has a BMAD scaffold and a package skeleton but no working development environment. Nothing
can be implemented or verified until `pytest`, `ruff`, and `mypy` run cleanly on a pinned Python.
This story establishes that floor so every later story can produce the test evidence Rule 50
demands.

## Acceptance criteria

1. A documented virtualenv recipe using Python 3.11 or 3.12 (not the system 3.14) exists in
   `README.md`, and following it produces a working environment.
2. `pyproject.toml` declares the package, its runtime dependencies, and its dev dependencies, with
   `ruff`, `mypy`, and `pytest` configured in that single file — no competing config files.
3. `pytest -q` runs and passes, collecting at least one real test (a placeholder that asserts the
   package imports and that `regex` grapheme matching is available).
4. `ruff check .` and `ruff format --check .` pass on the existing tree.
5. `mypy lipikar` passes on the existing tree.
6. `requirements-dev.txt` or equivalent pins exact versions so the environment is reproducible
   (R00.9).

## Technical notes

**Do not add torch in this story.** Runtime ML dependencies arrive with the stories that need them;
adding an unused 2GB dependency now makes CI slow and the environment fragile. This story installs
only what the metric and text layers require.

Dependencies for now:
- runtime: `regex` (required — `re` cannot match grapheme clusters, see R20.2), `numpy`,
  `pyyaml`, `pydantic` *(only if the Architect's config loader needs it; otherwise omit)*
- dev: `pytest`, `pytest-cov`, `ruff`, `mypy`

**Files to create or modify:**
- `pyproject.toml` — package metadata, `[tool.ruff]`, `[tool.mypy]`, `[tool.pytest.ini_options]`
- `requirements-dev.txt` — pinned dev deps
- `tests/unit/test_smoke.py` — the placeholder test
- `README.md` — the setup recipe section

**Ruff and mypy settings expected:**
```toml
[tool.ruff]
line-length = 100
target-version = "py311"

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_ignores = true
disallow_untyped_defs = true

[tool.pytest.ini_options]
testpaths = ["tests"]
```

**The smoke test must actually verify something (R10.9 / QA test-quality check):**
```python
def test_grapheme_matching_available() -> None:
    """regex must segment a Bengali conjunct as ONE cluster, not three codepoints."""
    import regex
    assert len(regex.findall(r"\X", "ক্ষ")) == 1
    assert len("ক্ষ") == 3          # documents why R20.2 exists
```
This is not a trivial placeholder: it fails if `regex` is missing or if someone swaps it for `re`,
which is exactly the regression that would silently break every metric in the project.

## Rules that apply
- R10.1 — Python 3.11/3.12, single config in `pyproject.toml`
- R10.9 — tests in `tests/unit` run in under 30 seconds, no disk, no network, no GPU
- R00.9 — reproducibility: pinned versions
- R20.2 — the reason `regex` is a hard requirement rather than a convenience

## Testing requirements
- `tests/unit/test_smoke.py::test_package_imports` — every stage subpackage imports
- `tests/unit/test_smoke.py::test_grapheme_matching_available` — as above
- Command: `pytest -q`

## Out of scope
- torch, transformers, opencv, or any ML dependency
- CI configuration (story 1.7)
- The actual metric implementation (story 1.3)
- Pre-commit hooks

## Definition of Done
Rule 50 — universal section only. No text handling, recognition path, or data sections apply.

---

## Dev section

**Completion notes:**

Python 3.12.10 installed alongside the system 3.14 (`winget install Python.Python.3.12`); `py -3.12`
now resolves. `.venv` created from it, package installed editable with dev extras. All six
acceptance criteria addressed:

1. Recipe in `README.md` §Setup and `docs/SETUP.md` §3 followed end to end on this machine.
2. `pyproject.toml` is the single config file — ruff, mypy, pytest and coverage all live in it. No
   `setup.cfg`, `.ruff.toml`, `mypy.ini`, or `pytest.ini` exists.
3. `pytest -q` collects 8 tests and passes; the grapheme test asserts real behaviour.
4. `ruff check .` and `ruff format --check .` both clean.
5. `mypy lipikar` clean across 7 source files.
6. `requirements-dev.txt` pins every transitive dependency exactly, generated from the working venv.

`torch` was not added, per the story's technical note.

**Files changed:**
- `pyproject.toml` — new; package metadata, deps, `[tool.ruff]`, `[tool.mypy]`,
  `[tool.pytest.ini_options]`, `[tool.coverage.*]`
- `requirements-dev.txt` — new; exact pins (R00.9)
- `tests/unit/test_smoke.py` — new; import test parametrized over all seven modules, plus the
  grapheme-cluster test
- `tests/integration/.gitkeep`, `notebooks/.gitkeep` — new; directories survive a clone
- `docs/stories/1.1.dev-environment-and-toolchain.md` — status, this section, two rule-ID fixes

**Test run output:**
```
########## pytest -q ##########
........                                                                 [100%]
8 passed in 0.03s
pytest_exit=0

########## ruff check . ##########
All checks passed!
ruff_check_exit=0

########## ruff format --check . ##########
9 files already formatted
ruff_fmt_exit=0

########## mypy lipikar ##########
Success: no issues found in 7 source files
mypy_exit=0

########## coverage ##########
Name                             Stmts   Miss  Cover
----------------------------------------------------
lipikar\__init__.py                  2      0   100%
lipikar\binnas\__init__.py           1      0   100%
lipikar\fard\__init__.py             1      0   100%
lipikar\kagoj\__init__.py            1      0   100%
lipikar\nazir\__init__.py            1      0   100%
lipikar\shirorekha\__init__.py       1      0   100%
lipikar\shuddhi\__init__.py          1      0   100%
----------------------------------------------------
TOTAL                                8      0   100%
Required test coverage of 70.0% reached. Total coverage: 100.00%
8 passed in 0.21s
```
Coverage is 100% only because the package is still seven docstrings. It is a wired floor, not an
achievement.

**Rule 50 self-check:**
- Full suite green, output pasted above (R50.0) — yes, nothing skipped, nothing xfailed
- `ruff check .` and `mypy lipikar` clean — yes
- No inline comments in `lipikar/` or `tests/`; one docstring in the grapheme test naming R20.2 — yes
- No hardcoded values — the module list in `test_smoke.py` is test data, not stage configuration
- No user-facing strings added — yes; nothing in this story prints to a user
- No secrets, no deed content — yes; privacy guard run on all five new paths, exit 0
- Scope held — no torch, no CI, no metric, no pre-commit hook

**Deviations from story:**

1. **`extend-exclude = ["*.md"]` added to `[tool.ruff]`.** ruff 0.16 formats Python code blocks
   inside Markdown by default, which failed AC4 on four files. Two of them must not be rewritten:
   `docs/architecture.md` is frozen spec that Dev may not edit, and `.bmad/rules/20-bengali-text.md`
   deliberately aligns a correct line against a `# WRONG` line so the contrast is visible — a
   formatter would destroy the teaching. Docs snippets are illustrative, not compiled.
2. **`[tool.setuptools.package-data]` declares `resources/strings/*.yaml`.** No such file exists
   yet, so this is declarative only. Added now because `lipikar/resources/` is not a package and the
   locale files would silently fail to ship when story 1.x creates them.
3. **`[tool.coverage.report] fail_under = 70`** wires the `min_test_coverage` gate from
   `core-config.yaml`. `--cov` is deliberately *not* in default `addopts`, so `pytest -q` stays fast
   for the R10.9 30-second inner loop; CI runs `pytest --cov` in story 1.7.
4. `requires-python = ">=3.11,<3.13"` upper-bounds the interpreter so the 3.14 trap (ADR-001) fails
   at install time rather than at first torch import.

**Discovered work filed:**
- **Story 1.2 — typed config loader** (new, added to epic 001). Rule 70 Part A has no owning story,
  and stories 1.4, 1.5 and 1.6 all read from `configs/`. See the epic's story table.
- **String resources** (`en.yaml`/`bn.yaml` + key-parity test) also has no owning story; added to
  the epic table as 1.9 with a note that R70.15 exempts log messages, so nothing in epic 001
  actually blocks on it.
- **Rotated marginal text** is unhandled in `docs/architecture.md` §2.2 — raised with the Architect,
  not filed as a story, because it is a spec change requiring human approval.

---

## QA section
*(QA agent fills this in. Gate: `docs/qa/gates/1.1.md`.)*

**Gate decision:** **PASS** — reviewed by QA (Sharmin), 2026-08-21, commit `8c98c2a`.

All six acceptance criteria met and verified independently. I did not accept the pasted output: I
re-ran `pytest -q` (102 passed), `ruff check .`, `ruff format --check .` and `mypy lipikar` myself,
and I rebuilt the 3.11 leg from scratch by following the README recipe in a throwaway venv, which
also produced 102 passed with clean lint and types. Confirmed no competing config files exist
(`setup.cfg`, `.ruff.toml`, `mypy.ini`, `pytest.ini`, `tox.ini`, `setup.py` all absent), and that
`test_grapheme_matching_available` is a real test that fails if `regex` is swapped for `re`.

**Findings:** two, both low severity, neither blocking and neither requiring a follow-up story.

- low — `README.md:64`: the recipe runs `pip install -e .` with dependency resolution, while
  `.github/workflows/ci.yml:37` deliberately uses `--no-deps`. Because `pyproject.toml:13-17`
  declares open ranges, a local environment can drift past the pins CI tests (R00.9). Match CI.
- low — `tests/unit/test_smoke.py:19`: `assert ... is not None` carries no information; the real
  check is the implicit `ImportError`.

Status moved to **Done**.
