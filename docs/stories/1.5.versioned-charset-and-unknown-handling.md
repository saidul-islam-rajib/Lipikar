# Story 1.5 — Versioned charset and unknown handling

**Status:** Review
**Epic:** 001 — Foundation and measurement
**Stage:** shirorekha
**Depends on:** 1.2, 1.3
**Branch:** `story/1.5-versioned-charset-and-unknown-handling`

## Context
An alphabet inferred from whichever characters happened to appear in a training split is a silent
trap: the first unseen conjunct or currency mark becomes an unrepresentable target, and nobody finds
out until a training run has already been poisoned. The charset is therefore a versioned file, and
every character outside it is announced.

## Acceptance criteria

1. The alphabet loads from a versioned file in `configs/`, never inferred from data (R20.5).
2. Coverage includes Bengali consonants, independent vowels, vowel signs, hasanta, nukta, Bengali
   digits ০–৯ **and** ASCII 0–9, ASCII letters, currency and fraction marks, the danda, and space.
3. An out-of-charset character maps to the configured unknown token **and** its codepoint is logged.
4. Encode/decode round-trips text that is inside the alphabet.
5. A version mismatch between the file and the config raises.
6. A missing charset file raises.

## Technical notes

The alphabet is codepoint-level, not cluster-level, because it is a CTC output alphabet: the model
emits vowel signs and hasanta as separate symbols and the clusters are reassembled by the text
layer. Charset file contents pass through `normalize` on load, so the file and the runtime text
agree — this matters for `ড় ঢ় য়`, which are composition-excluded and therefore live as base plus
nukta after NFC. That is why nukta (U+09BC) is a required member in its own right.

The unknown-character log records **only** the codepoint, never the surrounding text — a log line
carrying deed content would defeat the whole privacy posture (R30.1, architecture §7).

**Files:** `lipikar/shirorekha/charset.py`, `configs/charset.v1.yaml`, `configs/default.yaml`,
`tests/unit/test_charset.py`

## Rules that apply
- R20.5 Declare the charset explicitly · R70.2 Charset is configuration · R10.6 Fail loudly
- R10.7 Logging, not printing · R30.1 No deed content anywhere it can leak

## Testing requirements
`tests/unit/test_charset.py` — a parametrized coverage test naming each required category, round
trip, unknown-character logging asserted via `caplog`, decode bounds, version mismatch, missing file.

## Out of scope
- The CTC blank symbol and any decoder (Epic 006)
- Charset v2 — adding characters is a new versioned file, never an edit in place

## Definition of Done
Rule 50 — universal **and** text-handling sections.

---

## Dev section

**Completion notes:**
The coverage test is parametrized with the category name as the assertion message, so a failure says
which R20.5 category is missing rather than just showing a false assertion.

The unknown-character test asserts the literal `U+6F22` appears in the captured log, which pins both
halves of AC3: the mapping *and* the codepoint being recorded.

**Files changed:** as listed above.

**Test run output:**
```
tests/unit/test_charset.py .....................                         [100%]
21 passed
```

**Rule 50 self-check:** no hardcoded alphabet anywhere in `lipikar/`; unknown token comes from
config; logs carry codepoints only. Full suite green.

**Deviations from story:** none.

**Discovered work filed:**
- The charset covers the categories R20.5 requires, but it has not been validated against real deed
  pages, which do not exist in the repo yet. Confirming coverage against a real corpus belongs to
  Epic 003, and the logged unknowns are the mechanism that will surface the gaps.

---

## QA section
*(QA agent fills this in. Gate: `docs/qa/gates/1.5.md`.)*

**Gate decision:**
**Findings:**
