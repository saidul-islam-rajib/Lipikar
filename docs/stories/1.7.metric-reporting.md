# Story 1.7 — Metric reporting

**Status:** Done
**Epic:** 001 — Foundation and measurement
**Stage:** shuddhi
**Depends on:** 1.4, 1.6
**Branch:** `story/1.7-metric-reporting`

## Context
A mean CER of 0.18 can describe a system that reads most pages well and three pages not at all, or
one that reads every page mediocrely. Those are different projects with different next actions, and
the mean cannot tell them apart. Reporting therefore emits a distribution, and there is no API that
returns a bare mean.

## Acceptance criteria

1. `summarize` returns sample size, mean, median, configured percentiles, and the worst N samples.
2. Worst samples are ordered worst-first and capped at the configured N.
3. Ties break deterministically so a report is reproducible (R10.8).
4. Summarizing an empty sample set raises rather than reporting `nan` or `0`.
5. An out-of-range percentile raises.

## Technical notes

Percentiles use the **nearest-rank** definition, so every reported percentile is an actually
observed score rather than an interpolation between two pages. `percentiles` is configured as
`[90, 95]` and deliberately excludes 50: the median is reported in its own field, and reporting both
a `median` and a `p50` that disagree by definition would be a confusing defect.

Worst-N carries `item_id`, because "the ten worst predictions" (R40.7) is only actionable if you can
find the pages.

**Files:** `lipikar/shuddhi/reporting.py`, `configs/default.yaml`, `tests/unit/test_reporting.py`

## Rules that apply
- R40.7 Look at the worst predictions · R10.8 Determinism · R70.2 Thresholds are configuration

## Testing requirements
`tests/unit/test_reporting.py` — full distribution over a known sample, worst-N ordering and cap,
tie-breaking, empty set, invalid percentile.

## Out of scope
- Rendering a report for a human to read — that is user-facing text and needs string resources
  (story 1.9) plus a surface to render into (Epic 009)
- Reliability diagrams and confidence calibration (R40.8, Epic 009)

## Definition of Done
Rule 50 — universal section only.

---

## Dev section

**Completion notes:**
`MetricSummary` has no `__str__` and there is no formatting helper, which is deliberate: the moment
this module formats a sentence it owes R70.8 a string resource. It returns data; a surface renders
it.

Changing the configured percentiles from `[50, 90]` to `[90, 95]` during implementation removed a
real trap — with ten samples, `statistics.median` gives 0.45 while a nearest-rank p50 gives 0.40, and
shipping both numbers in one report invites someone to treat the difference as signal.

**Files changed:** as listed above.

**Test run output:**
```
tests/unit/test_reporting.py .....                                       [100%]
5 passed
```

**Rule 50 self-check:** no bare-mean API; worst-N and sample size always present; no user-facing
strings. Full suite green.

**Deviations from story:** none.

**Discovered work filed:** none.

---

## QA section
*(QA agent fills this in. Gate: `docs/qa/gates/1.7.md`.)*

**Gate decision:** **PASS** — reviewed by QA (Sharmin), 2026-08-21, commit `2000284`.

All five acceptance criteria met, 100% line coverage on the module, and I confirmed the coverage is
real by mutating every behaviour the story claims: reversing the worst-N order, replacing the
percentile computation with the mean (i.e. faking a distribution), and returning NaN instead of
raising on an empty set were all caught, the first two by the integration test as well. I
re-derived the reported statistics by hand for the ten-sample fixture — mean 0.45, median 0.45,
nearest-rank p90 0.8, p95 0.9 — and the code returns exactly those. R40.6 is satisfied structurally:
`MetricSummary` is the only return type, every field is mandatory, and there is no API that can
produce a bare mean. Returning data with no `__str__` and no formatting helper is the right call —
formatting would owe R70.8 a string resource.

**Findings:** three, all low, none blocking and none requiring a follow-up story.

- low — `reporting.py:1` cites **R40.7**; the distribution rule is **R40.6** (R40.7 is qualitative
  review, a fair second citation for the worst-N field).
- low — `reporting.py:23-29`: `MetricSummary` carries no link to the eval set it was computed over
  (no manifest checksum, no config hash), so two summaries from different eval-set versions are
  indistinguishable once they leave the process (R40.6, R70.6). Latent today; cheapest to add before
  Epic 006 produces numbers. Recorded again in the 1.6 gate so it is picked up with Story 1.14.
- low — `tests/unit/test_reporting.py`: no single-sample case and no case where `worst_n` exceeds
  the sample count. Both behave correctly today.

**Cross-story note:** the worst-N ranking will be computed over decoder output, which story 1.5's
five-cluster `<unk>` token inflates. The fix belongs in 1.5 (Story 1.13); the consequence lands here.

Status moved to **Done**.
