# Copy this file to .env.local (in this same folder) and fill in real values.
# .env.local is gitignored — never commit it.

# Which provider is used by default in the UI. One of: claude, openai, gemini
DEFAULT_PROVIDER=claude

# Only the key(s) for the provider(s) you want to use are required.
# A provider with no key is shown as unconfigured in the UI and cannot be selected.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-5

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o

GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

# Admin login (protects /admin). Pick any username/password for local use.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me

# Random secret used to sign the admin session cookie. Generate one with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=
